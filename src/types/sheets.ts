export type SheetProject = {
  name: string;
  type: string;
  description: string;
  imageUrl: string;
  link?: string;
  tags?: string[];
};

export type SheetTestimonial = {
  quote: string;
  name: string;
  role: string;
  avatar: string;
};
