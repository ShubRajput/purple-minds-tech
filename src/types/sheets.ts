export type SheetProject = {
  name: string;
  type: string;
  description: string;
  imageUrl: string;
};

export type SheetTestimonial = {
  quote: string;
  name: string;
  role: string;
  avatar: string;
};
